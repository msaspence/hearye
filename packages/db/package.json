{
  "name": "@hearye/db",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "build": "rm -rf ./dist && swc ./src --out-dir ./dist --config-file .swcrc",
    "migrate": "knex migrate:latest --knexfile ./src/knexconfig.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^2.3.0",
    "objection": "^3.0.1",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.8",
    "ts-node": "^10.9.1"
  }
}
