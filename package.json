{
  "name": "hearye",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:api": "npm run build:db && npm run build:env && npm run build --workspace @hearye/api",
    "build:dayjs": "npm run build --workspace @hearye/dayjs",
    "build:db": "npm run build:dayjs && npm run build --workspace @hearye/db",
    "build:env": "npm run build --workspace @hearye/env",
    "build:reminder-runner": "npm run build --workspace @hearye/reminder-runner",
    "dev:api": "npm run dev --workspace @hearye/api",
    "dev:reminder-runner": "npm run dev --workspace @hearye/reminder-runner",
    "migrate": "npm run migrate --workspace @hearye/db",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:api": "npm run start --workspace @hearye/api",
    "start:reminder-runner": "npm run start --workspace @hearye/reminder-runner",
    "tunnel": "lt --subdomain hearye --port 8080"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc-node/sourcemap-support": "^0.2.0",
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.248",
    "@tsconfig/recommended": "^1.0.1",
    "@types/debug": "^4.1.7",
    "@typescript-eslint/eslint-plugin": "^5.36.2",
    "eslint": "^8.23.1",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-jest": "^27.0.4",
    "eslint-plugin-lodash": "^7.4.0",
    "jest": "^29.0.3",
    "localtunnel": "^2.0.2",
    "prettier": "^2.7.1",
    "tsconfig-paths": "^4.1.0",
    "tslib": "^2.4.0"
  },
  "workspaces": [
    "apps/api",
    "apps/reminder-runner",
    "packages/dayjs",
    "packages/db",
    "packages/env"
  ],
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
