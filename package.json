{
  "name": "hearye",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:api": "npm run build:logger && npm run build:env && npm run build:db && npm run build --workspace @hearye/api",
    "build:dayjs": "npm run build:logger && npm run build --workspace @hearye/dayjs",
    "build:db": "npm run build:logger && npm run build:env && npm run build:dayjs && npm run build --workspace @hearye/db",
    "build:env": "npm run build --workspace @hearye/env",
    "build:logger": "npm run build --workspace @hearye/logger",
    "build:reminder-runner": "npm run build:logger && npm run build:env && npm run build:db && npm run build --workspace @hearye/reminder-runner",
    "build:site": "npm run build --workspace @hearye/site",
    "dev:api": "npm run dev --workspace @hearye/api",
    "dev:reminder-runner": "npm run dev --workspace @hearye/reminder-runner",
    "dev:site": "npm run dev --workspace @hearye/site",
    "migrate": "npm run migrate --workspace @hearye/db",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:api": "npm run start --workspace @hearye/api",
    "start:reminder-runner": "npm run start --workspace @hearye/reminder-runner",
    "start:site": "npm run start --workspace @hearye/site",
    "tunnel": "./loophole http 8080 --hostname hearye"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@sentry/cli": "^2.8.0",
    "@swc-node/sourcemap-support": "^0.2.0",
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.248",
    "@tsconfig/recommended": "^1.0.1",
    "@types/debug": "^4.1.7",
    "@typescript-eslint/eslint-plugin": "^5.36.2",
    "eslint": "^8.23.1",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-jest": "^27.0.4",
    "eslint-plugin-lodash": "^7.4.0",
    "jest": "^29.0.3",
    "localtunnel": "^2.0.2",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.0",
    "tslib": "^2.4.0"
  },
  "workspaces": [
    "apps/api",
    "apps/reminder-runner",
    "apps/site",
    "packages/dayjs",
    "packages/db",
    "packages/env",
    "packages/logger"
  ],
  "dependencies": {
    "@immobiliarelabs/fastify-sentry": "^5.0.0-next.1",
    "@logtail/node": "^0.1.12",
    "@logtail/winston": "^0.1.13",
    "@sentry/node": "^7.16.0",
    "@sentry/tracing": "^7.16.0",
    "dotenv": "^16.0.2",
    "fastify-plugin": "^4.3.0",
    "winston": "^3.8.2"
  }
}
